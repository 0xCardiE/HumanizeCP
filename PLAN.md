So I want to make a chrome extension that will add right click to the browser where we can copy the selected text and auto clean it from usual AI tell signs.
Here is most common one list

https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai


so we should try and auto replace things like dash sign.  Also have settings list where we include or remove each thing. Should be simple extension where we just have settings and this functionality.  This cleanup code should aim to not change the text much but just replace obvious signs and keep the same meaning and structure, not change whole sentences.

Also add git to this and track changes. In the end compile it so we can try it. 
